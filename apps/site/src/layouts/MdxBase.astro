---
import Base from '@layouts/Base.astro';

interface Props {
  frontmatter: {
    title: string;
    enableRecaptcha?: boolean;
  };
}

const { frontmatter }: Props = Astro.props;
---

<Base title={frontmatter.title} enableRecaptcha={frontmatter.enableRecaptcha}>
  <slot />
</Base>
